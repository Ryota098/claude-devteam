# Devteam Workspace Rules

- セッションは使い捨てにし、継続状態は `.devteam/` に保存する。
- 新しいセッションでは、まず `shared/workflow.md`、`shared/session-handoff.md`、`pm/current-task.md` を確認する。
- 推測は事実と分けて記録する。
- `current-*` は最新状態を保つ。
- 重要な判断は `shared/decisions.md` に残す。
- 会話全文ではなく、結論・未解決事項・次アクションのみを保存する。
